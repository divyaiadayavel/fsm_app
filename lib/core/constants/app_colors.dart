import 'package:flutter/material.dart';

class AppColors {
  // 🔥 PRIMARY (Main Brand - Using your hex 0xFF4F46E5)
  static const primary = Color(0xFF4F46E5);
  static const secondary = Color(0xFF3B82F6); // Using your blue for secondary

  // 🌈 LOGIN GRADIENT (Adjusted to Blue/Indigo)
  static const backgroundTop = Color(0xFF4F46E5);
  static const backgroundBottom = Color(0xFF3B82F6);

  // 🌗 LIGHT MODE
  static const backgroundLight = Color(0xFFF5F5F5);
  static const cardLight = Colors.white;

  // 🌙 DARK MODE
  static const backgroundDark = Color(0xFF121212);
  static const cardDark = Color(0xFF1E1E1E);

  // 🧾 COMMON
  static const card = Colors.white;
  static const white = Colors.white;
  static const background = Color(0xFFF5F5F5);

  // 📝 TEXT
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF6B7280); // Using your grey

  // 🔘 BORDER
  static const border = Color(0xFFE0E0E0);

  // 🎨 EXTRA PALETTE
  static const blue = Color(0xFF3B82F6);
  static const pink = Color(0xFFEC4899);
  static const green = Color(0xFF10B981);
  static const orange = Color(0xFFF59E0B);
  static const grey = Color(0xFF6B7280);

  // ⚠️ STATUS COLORS
  static const success = Color(0xFF10B981); // Matches your green
  static const warning = Color(0xFFF59E0B); // Matches your orange
  static const error = Colors.red;
}
